# —— Worker 供应商密钥（便宜、够用的基础编码/测试/文档模型）——
# 指挥大师(orchestrator/planner)默认用你本机 `claude login` 的订阅，无需 key。
# worker/reviewer 用下面的第三方 Anthropic 兼容端点。

# GLM-5.2（Z.ai）—— 默认 coder
GLM_API_KEY=
GLM_BASE_URL=https://api.z.ai/api/anthropic
GLM_MODEL=glm-5.2

# Kimi（Moonshot）—— 默认 reviewer（跨模型审）
KIMI_API_KEY=
KIMI_BASE_URL=https://api.moonshot.ai/anthropic
KIMI_MODEL=kimi-k2.5

# —— 行为 ——
# 无 key 时可用 mock 供应商本地跑通编排（不真调模型）：把 coder/reviewer 设成 mock
# 见 loop-engineer.config.json
